// UI

import './LoginForm.css';

import React from 'react';

// not use Component this time
// use the technique of destrction in es6
// onSubmit... are functions defined in LoginPage
// html needs to paste
const LoginForm = ({
    onSubmit,
    onChange,
    errors,
    user
}) => {
    <div className="container">
    </div>
}